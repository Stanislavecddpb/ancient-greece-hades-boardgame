{
  "name": "cyclades",
  "version": "0.0.1",
  "private": true,
  "description": "Браузерная версия настольной игры про господство над островами Эгейского моря",
  "workspaces": [
    "packages/engine",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "build": "npm run build -w packages/engine && npm run build -w packages/server && npm run build -w packages/client",
    "dev:server": "npm run dev -w packages/server",
    "dev:client": "npm run dev -w packages/client",
    "test": "npm run test -w packages/engine"
  },
  "devDependencies": {
    "puppeteer": "^25.1.0"
  }
}
